# SQL 与 JDBC 练习

8 月 17 日前不要提前填写答案。

1. 在 `schema.sql` 中手写 `users`、`activities`、`registrations` 三张表。
2. 添加主键、外键、非空约束和活动/用户组合唯一约束。
3. 在 `data.sql` 中手写至少 20 条有业务意义的示例数据。
4. 在 `queries.sql` 中完成至少 15 条查询，包括筛选、排序、分页、聚合和 JOIN。
5. 在 `transaction-notes.md` 中解释报名操作为什么需要事务。
6. JDBC 密码从环境变量读取，不提交真实密码。

