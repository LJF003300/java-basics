# 2026-08-03 学习日志

## 已完成

- 使用 `String`、`int`、`double` 和 `boolean` 保存并输出数据。
- 使用 `Scanner` 接收整数和小数输入，并在结束时关闭输入工具。
- 完成温度转换器，验证输入 `0`、`100`、`-40` 时结果正确。
- 完成 BMI 计算器，验证体重 `60` 千克、身高 `1.75` 米时结果约为 `19.5918`。
- 将不同的公共类拆分到同名 `.java` 文件中。

## 验证证据

- `mvn test`：构建成功，1 个已有测试通过，0 失败、0 错误。
- Java 编译器成功编译 6 个源文件。

## 已理解的关键点

- 变量由类型、名称和值组成，赋值后可以继续更新。
- `println` 的字符串拼接结果仍作为一个实参传入。
- 整数除法会丢弃小数；表达式中出现 `double` 后按浮点数计算。
- 一个顶层 `public` 类应放在与类名相同的 `.java` 文件中。

## 下一步

- 学习比较运算、布尔表达式、`if/else`、循环和方法。

